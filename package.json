{
  "name": "easy-property-management",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && VITE_API_URL=/api/v1 npm run build && cd ../backend && npm install && npx prisma generate && npm run build",
    "start": "cd backend && npx prisma migrate deploy && node dist/index.js",
    "seed": "cd backend && npx tsx prisma/seed.ts"
  }
}
